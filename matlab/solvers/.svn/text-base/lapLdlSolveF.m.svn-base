function f = lapLdlSolveF(l,d)
% function f = lapLdlSolveF(l,d)
%
% given an ldl factorization, applies it to solve for x
% returns a function f such that f(b) does this

f = @(b)(lapLdlSolve(b,l,d));
